//const auth = require('../../middleware/authorization');
/* const passport = require('passport');
//more imports?
//to get to the auth endpoints we need to send the token with a request to the backend
//in the authorization header (can be added in options to axios)
//you can get the token from window.localStorage.getItem('token')
const requireAuth = passport.authenticate('jwt', {session : false});

//if we want to only let logged in users reach some end points!
app.get('/stuff', auth.requireAuth, (req,res,next)=>{

}); */