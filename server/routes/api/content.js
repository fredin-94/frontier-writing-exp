/* const passport = require('passport');
//more imports?

const requireAuth = passport.authenticate('jwt', {session : false});

//if we want to only let logged in users reach some end points!
app.get('/stuff', requireAuth, (req,res,next)=>{

}); */